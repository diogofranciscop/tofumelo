function loadPage(a,n){let t=window.innerWidth<=768?10:20,e=a*t,i=Math.min(e+t,n.length),o=n.slice(e,i);$("#post-container").empty(),loadRecipes(o),createPaginationButtons(a,n,t)}function loadRecipes(a){let n=$("#post-container");n.empty();let t=window.innerWidth<=768?4:8;a.forEach((a,e)=>{let i=$(`
    <div class="card loading-card">
        <div class="card__spinner">
            <div class="spinner"></div>
        </div>
    </div>
`);n.append(i);let o=a.image.replace(/\.(webp|png|jpg|jpeg)$/,"-180px.$1"),d=new Image;d.src=o,d.onload=()=>{if(i.parent().length){let n='loading="lazy"';e<t&&(n="");let d="yes"===a.new?'<div class="new-tape">Nova Receita</div>':"",l=$(`
            <a href="${a.url}" class="card">
                ${d}
                <div class="card__overlay">
                    <p>${a.description}</p>
                </div>
                <div class="card__img-container">
                    <img src="${o}" class="card__img" alt="${a.title}" ${n}>
                </div>
                <div class="card__footer">
                    <span class="title-card">${a.title.toUpperCase()}</span>
                </div>
            </a>
        `);i.replaceWith(l)}},d.onerror=()=>{i.parent().length&&i.replaceWith(`
            <div class="card error-card">
                <p>Failed to load the recipe image.</p>
            </div>
        `)}})}function createPaginationButtons(a,n,t){let e=Math.ceil(n.length/t);$("#pagination-container").empty();let i=$("<button>").text("PRIMEIRA P\xc1GINA").addClass("pagination-button").on("click",()=>{loadPage(0,n),$(window).scrollTop(0)}),o=$("<button>").text("\xdaLTIMA P\xc1GINA").addClass("pagination-button").on("click",()=>{loadPage(e-1,n),$(window).scrollTop(0)}),d=$("<button>").text("←").addClass("pagination-button").on("click",()=>{loadPage(Math.max(0,a-1),n),$(window).scrollTop(0)}),l=$("<button>").text("→").addClass("pagination-button").on("click",()=>{loadPage(Math.min(e-1,a+1),n),$(window).scrollTop(0)});$("#pagination-container").append(i,d),a>2&&$("#pagination-container").append($("<span>").text("1").addClass("pagination-button").on("click",()=>loadPage(0,n))),a>3&&$("#pagination-container").append($("<span>").text("...").addClass("pagination-ellipsis"));for(let s=Math.max(0,a-1);s<=Math.min(e-1,a+1);s++){let p=$("<button>").text(s+1).addClass("pagination-button");s===a&&p.addClass("active"),p.on("click",()=>{loadPage(s,n),$("html, body").animate({scrollTop:0},400,"linear")}),$("#pagination-container").append(p)}a<e-4&&$("#pagination-container").append($("<span>").text("...").addClass("pagination-ellipsis")),a<e-3&&$("#pagination-container").append($("<span>").text(e).addClass("pagination-button").on("click",()=>loadPage(e-1,n))),$("#pagination-container").append(l,o)}document.addEventListener("DOMContentLoaded",function(){if(!$("#spinner-styles").length){let a=`
    <style id="spinner-styles">
        .loading-card {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 270px; /* Adjust height as per your card design */
            border: 1px solid #ddd;
            margin: 10px;
            width: 200px;
        }
        @media(max-width: 800px) {
            .loading-card {
                height: 190px;
                width: 140px;
            }
        }
        .card__spinner {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            width: 100%;
        }
        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid rgba(84, 130, 76, 0.2);
            border-top: 4px solid #54824c;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            from {
                transform: rotate(0deg);
            }
            to {
                transform: rotate(360deg);
            }
        }
    </style>
`;$("head").append(a)}}),document.addEventListener("DOMContentLoaded",function(){document.getElementById("loader")});