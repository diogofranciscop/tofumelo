function loadPage(a,t){let n=window.innerWidth<=768?10:20,e=a*n,i=Math.min(e+n,t.length),d=t.slice(e,i);$("#post-container").empty(),loadRecipes(d),createPaginationButtons(a,t,n),$(window).scrollTop(0)}function loadRecipes(a){let t=$("#post-container");t.empty();let n=window.innerWidth<=768?4:8;a.forEach((a,e)=>{let i=$(`
    <div class="card loading-card">
        <div class="card__spinner">
            <div class="spinner"></div>
        </div>
    </div>
`);t.append(i);let d=a.image.replace(/\.(webp|png|jpg|jpeg)$/,"-180px.$1"),o=new Image;o.src=d,o.onload=()=>{if(i.parent().length){let t='loading="lazy"';e<n&&(t="");let o="yes"===a.new?'<div class="new-tape">Nova Receita</div>':"",s=$(`
            <a href="${a.url}" class="card">
                ${o}
                <div class="card__overlay">
                    <p>${a.description}</p>
                </div>
                <div class="card__img-container">
                    <img src="${d}" class="card__img" alt="${a.title}" ${t}>
                </div>
                <div class="card__footer">
                    <span class="title-card">${a.title.toUpperCase()}</span>
                </div>
            </a>
        `);i.replaceWith(s)}},o.onerror=()=>{i.parent().length&&i.replaceWith(`
            <div class="card error-card">
                <p>Failed to load the recipe image.</p>
            </div>
        `)}})}function createPaginationButtons(a,t,n){let e=Math.ceil(t.length/n);$("#pagination-container").empty();let i=$("<button>").text("PRIMEIRA P\xc1GINA").addClass("pagination-button").on("click",()=>loadPage(0,t)),d=$("<button>").text("\xdaLTIMA P\xc1GINA").addClass("pagination-button").on("click",()=>loadPage(e-1,t)),o=$("<button>").text("←").addClass("pagination-button").on("click",()=>loadPage(Math.max(0,a-1),t)),s=$("<button>").text("→").addClass("pagination-button").on("click",()=>loadPage(Math.min(e-1,a+1),t));$("#pagination-container").append(i,o),a>2&&$("#pagination-container").append($("<span>").text("1").addClass("pagination-button").on("click",()=>loadPage(0,t))),a>3&&$("#pagination-container").append($("<span>").text("...").addClass("pagination-ellipsis"));for(let l=Math.max(0,a-1);l<=Math.min(e-1,a+1);l++){let p=$("<button>").text(l+1).addClass("pagination-button");l===a&&p.addClass("active"),p.on("click",()=>loadPage(l,t)),$("#pagination-container").append(p)}a<e-4&&$("#pagination-container").append($("<span>").text("...").addClass("pagination-ellipsis")),a<e-3&&$("#pagination-container").append($("<span>").text(e).addClass("pagination-button").on("click",()=>loadPage(e-1,t))),$("#pagination-container").append(s,d)}document.addEventListener("DOMContentLoaded",function(){if(!$("#spinner-styles").length){let a=`
    <style id="spinner-styles">
        .loading-card {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 270px; /* Adjust height as per your card design */
            border: 1px solid #ddd;
            margin: 10px;
            width: 200px;
        }
        @media(max-width: 800px) {
            .loading-card {
                height: 190px;
                width: 140px;
            }
        }
        .card__spinner {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            width: 100%;
        }
        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid rgba(84, 130, 76, 0.2);
            border-top: 4px solid #54824c;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            from {
                transform: rotate(0deg);
            }
            to {
                transform: rotate(360deg);
            }
        }
    </style>
`;$("head").append(a)}let t=window.innerWidth<=768?10:20,n=Math.ceil(allPosts.length/t);n>1&&createPaginationButtons(0,allPosts,t)});